spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: none
  datasource:
    url: jdbc:postgresql://localhost:5432/online_cv
    username: postgres
    password: mausimj
    driverClassName: org.postgresql.Driver